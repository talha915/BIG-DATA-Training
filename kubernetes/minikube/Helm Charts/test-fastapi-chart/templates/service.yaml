apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-fastapi-chart.fullname" . }}
  labels:
    {{- include "test-fastapi-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-fastapi-chart.selectorLabels" . | nindent 4 }}
